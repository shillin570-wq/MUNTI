# 复制为 .env 后填写（本地 build 或 CI/托管平台「环境变量」里同名配置均可）。
#
# ── VITE_SITE_URL（生产构建强烈建议填写）────────────────────────────
# 作用① 微信 / QQ / 企业微信等「链接卡片」预览：写入 og:url、og:image（须 https 可公网访问）
# 作用 ② 「转发分享」按钮生成的文案里「在线测试：」后面的链接：与当前路径拼接成规范地址
#        不填时：预览相关 meta 会在构建时被省略；转发链接仍用浏览器当前页 window.location.href
#
# 规则：完整 https 根地址，不要末尾斜杠。
# 示例：https://mun.example.com
#       https://user.github.io/repo-name
#
# 分享缩略图：使用 public/og-share.png（约 1200×630 横图，可替换占位图）
VITE_SITE_URL=
